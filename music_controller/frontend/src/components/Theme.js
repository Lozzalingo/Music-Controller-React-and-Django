import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primaryCustom: {
      main: "#425fff", // Custom primary color
    },
    secondaryCustom: {
      main: "#ff428e", // Custom secondary color
    },
    tertiaryCustom: {
      main: "#91e2ff", // Custom secondary color
    },
    quaternaryCustom: {
      main: "#CECECE", // Custom secondary color
    },
  },
});

export default Theme;
